version: '3.8'

services:
  app:
    build:
      context: .
      target: builder
    container_name: node-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"
    command: ["node", "server.js"]
